workflows:
  build-project:
    name: Build Bayaa POS for Windows and Android
    environment:
      flutter: stable
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build windows --release
      - flutter build apk --release
    artifacts:
      - build/windows/runner/Release/**
      - build/app/outputs/flutter-apk/*.apk
